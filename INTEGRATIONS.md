# 让 AI 上班，让鱼上钩

在「什么时候」里选一个 AI 客户端，再点「开始玩」。它工作时，桌面玩法会跟着变化。每次可以跟随一个客户端；同一客户端有几个任务一起跑时，等最后一个忙完才停。

## Codex

选择「跟随 Codex」，开始一个新任务即可。应用会从这台 Mac 上识别它的工作状态，无需额外配置。

## Qoder / WorkBuddy

1. 选择「跟随 Qoder」或「跟随 WorkBuddy」。
2. 点击「安装监听」。现有设置会保留，并自动备份。
3. 重启对应客户端。如果客户端提示确认新增配置，按提示启用。
4. 开始一个新任务，看看桌面有没有跟着热闹起来。

不想继续连接时，选择对应客户端，点击「移除」，然后重启客户端即可。移除不会影响你原来的其他设置。

目前针对 Qoder 和 WorkBuddy 的 Mac 桌面客户端。Qoder CN / 通义版、CodeBuddy CLI 暂不支持。请使用支持工作事件通知的客户端版本。

## 状态没跟上怎么办

连接功能还在测试中，Qoder / WorkBuddy 需要更多真实使用反馈。

- 确认本应用已经「开始玩」，选对了客户端。
- 安装监听后重启客户端，并开始一个新任务。
- 如果客户端要求确认配置，先完成确认。
- AI 长时间思考、等待你的确认，或者意外退出时，状态可能有延迟。长时间收不到变化后，应用会把它视为已经停工。
- 暂时连不上也没关系，切回「随时开启」就能继续玩。

[反馈连接问题](https://github.com/xiaoyan648/while-ai-works/issues)时，告诉我们客户端版本和发生了什么即可，不用上传私人对话或代码。

## 会读取或保存什么

Codex 通过本机工作记录识别状态；Qoder / WorkBuddy 通过客户端发出的通知识别状态。这些通知可能包含对话内容，应用只提取判断是否工作所需的信息，不保存对话或代码，也不上传数据。

关闭桌面效果后，应用停止跟随状态。已安装的 Qoder / WorkBuddy 连接仍会更新少量本机状态记录；需要彻底停止时，用上面的「移除」按钮即可。

所有鱼获、图鉴和累计记录都保存在自己的 Mac 上。
