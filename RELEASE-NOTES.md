# 0.14.1 测试版 · 一起上班，一起摸鱼

现在可以同时跟随 Codex、Qoder 和 WorkBuddy，默认全选。多个 AI 一起工作，桌面玩法也能跟上；一个任务结束，不会把其他任务一起算成下班。

## 这次更新

- 「什么时候」支持多选并保存选择，菜单栏与设置同步。
- 「安装监听 / 移除」收进「连接管理」，主界面更简洁。
- 按客户端和会话分别统计工作数量，再汇总繁忙程度；木鱼仍是整体工作时每 5 秒减 1，不会重复扣减。
- 修复 WorkBuddy 同一任务中消息 ID 变化，导致结束信号被忽略、完成后仍显示工作中的问题。
- 各来源连续 5 分钟没有有效事件时恢复空闲；正常结束信号到达后无需等待兜底。新工作事件到来会恢复状态。
- 修复 Codex 长任务在中途开启监听时，因日志尾部不含开始事件而漏算的问题。

## 下载与升级

适用于 Apple Silicon（M 系列）Mac，macOS 14 或更新版本。构建号 24，暂不支持 Intel Mac 和 Windows。

下载 `While-AI-Works-0.14.1-macOS-arm64.zip`，退出旧版后用解压出的应用替换。鱼获、图鉴、累计记录会保留。旧版单选设置首次升级为全选；原先明确选择「随时开启」的会保留该模式。

**已连接过 Qoder / WorkBuddy 的用户也请在「连接管理」重新点击对应客户端的「安装监听」，然后重启客户端。** 这会更新辅助程序，让结束事件修复与新的失活时间生效；原有其他设置和 Hooks 会保留。

[连接说明](https://github.com/xiaoyan648/while-ai-works/blob/main/INTEGRATIONS.md)

## 已验证与边界

- 生命周期、重复事件、并行会话、来源隔离、两种结束顺序、取消选择、偏好保存、5 分钟失活与恢复、Codex 长日志接入等自动化回归通过。
- WorkBuddy 和 Qoder 单客户端正常开始/结束此前已实测；新版多选界面和 Codex 长任务重新接入已实测。
- 新版 Qoder + Codex 双端完整实测尚未完成：Qoder 测试任务停在单次终端执行确认，自动化界面读取未能继续。不会把自动化协议验证等同于完整双端实测。
- 连续超过 5 分钟没有事件的纯思考或长工具调用，可能暂时显示空闲；等待权限、客户端漏报或乱序事件仍可能影响判断。繁忙度来自事件频率，不是 token/s。
- 当前为 ad-hoc 签名测试包，尚未进行 Developer ID 签名和 Apple 公证。附 `SHA256SUMS.txt` 供校验下载文件。

安装包只在本机识别工作状态，不上传对话、代码或工具参数。
